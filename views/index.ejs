<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sunset House Bot - Панель управления</title>
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
    <meta http-equiv="refresh" content="60">
</head>
<body>
    <div class="container">
        <header>
            <h1><i class="fas fa-robot"></i> Sunset House Bot</h1>
            <p class="subtitle">Панель управления ботом для хауса Sunset</p>
        </header>

        <main>
            <section class="status-card <%= stats.isRunning ? 'running' : 'stopped' %>">
                <div class="status-icon">
                    <i class="fas <%= stats.isRunning ? 'fa-circle-play' : 'fa-circle-stop' %>"></i>
                </div>
                <div class="status-info">
                    <h2>Статус: <%= stats.isRunning ? 'Работает' : 'Остановлен' %></h2>
                    <p>Время работы: <%= uptime %></p>
                    <p>Последняя активность: <%= stats.lastActivity ? stats.lastActivity.toLocaleString('ru-RU') : 'Нет данных' %></p>
                </div>
                <div class="status-actions">
                    <button id="restart-bot" class="btn btn-primary"><i class="fas fa-sync"></i> Перезапустить</button>
                    <button id="stop-bot" class="btn btn-danger" <%= stats.isRunning ? '' : 'disabled' %>><i class="fas fa-stop"></i> Остановить</button>
                </div>
            </section>

            <div class="stats-grid">
                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-hourglass-half"></i>
                    </div>
                    <div class="stat-info">
                        <h3>Ожидающие анкеты</h3>
                        <p class="stat-value"><%= stats.pendingForms %></p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-check-circle"></i>
                    </div>
                    <div class="stat-info">
                        <h3>Принятые анкеты</h3>
                        <p class="stat-value"><%= stats.acceptedForms %></p>
                    </div>
                </div>

                <div class="stat-card">
                    <div class="stat-icon">
                        <i class="fas fa-times-circle"></i>
                    </div>
                    <div class="stat-info">
                        <h3>Отклоненные анкеты</h3>
                        <p class="stat-value"><%= stats.rejectedForms %></p>
                    </div>
                </div>
            </div>

            <section class="help-section">
                <h2>Информация о боте</h2>
                <div class="help-card">
                    <h3>Команды бота</h3>
                    <div class="commands-list">
                        <div class="command">
                            <span class="cmd">/start</span>
                            <span class="desc">Начать заполнение анкеты</span>
                        </div>
                        <div class="command">
                            <span class="cmd">/cancel</span>
                            <span class="desc">Отменить заполнение анкеты</span>
                        </div>
                        <div class="command">
                            <span class="cmd">/list</span>
                            <span class="desc">Просмотр непринятых анкет (для админов)</span>
                        </div>
                        <div class="command">
                            <span class="cmd">/view_[ID]</span>
                            <span class="desc">Просмотр анкеты по ID (для админов)</span>
                        </div>
                        <div class="command">
                            <span class="cmd">/history_accepted</span>
                            <span class="desc">История принятых анкет (для админов)</span>
                        </div>
                        <div class="command">
                            <span class="cmd">/history_rejected</span>
                            <span class="desc">История отклоненных анкет (для админов)</span>
                        </div>
                    </div>
                </div>
            </section>
        </main>

        <footer>
            <p>&copy; <%= new Date().getFullYear() %> Хаус Sunset. Все права защищены.</p>
            <p>
                <a href="https://t.me/sunset_AccessBot" target="_blank">
                    <i class="fab fa-telegram"></i> @sunset_AccessBot
                </a>
            </p>
        </footer>
    </div>

    <script src="/js/main.js"></script>
</body>
</html> 